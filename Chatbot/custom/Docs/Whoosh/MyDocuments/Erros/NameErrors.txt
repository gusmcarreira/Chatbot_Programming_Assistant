O erro NameError é um que ocorre, dentre outros motivos, durante o uso ou declaração incorreta de variáveis.
-#-

O erro NameError ocorre, dentre outros motivos, durante o uso ou declaração incorreta de variáveis.

#Estará a tentar utilizar uma variável inexistente?

Exemplo:
y = 2
print(x)

Observe que a instrução print tenta imprimir o valor da variável x que não existe. Por isso o erro indica que essa variável não foi declarada.

Solução:
y = 2
print(y)

#Estará a tentar usar o nome de uma instrução corretamente?

Exemplo:
primt("Olá")

Os nomes das funções precisam ser escritos de forma igual à sua declaração. Observe que no código acima nós escrevemos primt ao invés de print, e por isso o Python não localizou isso, uma vez que ele não foi definido.

-#-






























