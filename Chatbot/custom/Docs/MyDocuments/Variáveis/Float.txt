Float, número decimal, ou número de ponto flutuante, é um número, positivo ou negativo, que contém uma ou mais décimas.

Exemplo:

<code>X = 20.5</code>

Note que na matemática do dia a dia usamos a virgula (,) para este efeito, no entanto Python segue a notação dos Estado Unidos em que se faz uso do ponto (.)

Sugestões:Como converter um dado em número decimal?<sep>Como obter o tipo de um dado?
-#-
Para converter um inteiro para float usa-se a função float().

Exemplo:

<code>
1  # Converte inteiro para float
2  x = 2
3  s = float(x)
4  print(s)

2.0
</code>

Sugestões:Como obter o tipo de um dado?